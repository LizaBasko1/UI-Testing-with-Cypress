/// <reference types="cypress" />

describe('First test suite', () => {

    describe('suite section', () =>{
        beforeEach('login', () =>{
            // repeat for every test in the section
        })

        it('first test', () => {
            // put the code of the test itself
        })

        it('second test', () => {
            // put the code of the test itself
        })
    })

    it('first test', () => {
        // put the code of the test itself
    })

    it('second test', () => {
        // put the code of the test itself
    })

    it('third test', () => {
        // put the code of the test itself
    })

    it('fourth test', () => {
        // put the code of the test itself
    })
})

describe('Second test suite', () => {
    it('first test', () => {
        // put the code of the test itself
    })

    it('second test', () => {
        // put the code of the test itself
    })

    it('third test', () => {
        // put the code of the test itself
    })

    it('fourth test', () => {
        // put the code of the test itself
    })
})